import numpy as np

# Create matrix
matrix = np.array([ [1, 2, 3],
                    [2, 2, 6],
                    [3, 8, 9] ])

# Return trace ->sum of principal diagonal call trace of a matrix
matrix.trace()
print(matrix.trace())
